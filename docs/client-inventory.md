# Client deploy inventory

Auto-maintained by `scripts/append-inventory.sh`. Manual edits are fine but rows for known slugs will be overwritten on the next deploy.

Status values: `active`, `destroyed`, `paused`.

| Slug | FQDN | IPv4 | Image tag | Deployed (UTC) | Status |
|------|------|------|-----------|----------------|--------|
